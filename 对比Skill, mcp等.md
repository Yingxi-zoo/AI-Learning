---
title: Claude Skills explained：结构化中文整理
source: https://claude.com/blog/skills-explained
source_title: Skills explained
publisher: Anthropic
date_read: 2026-09-21
tags:
  - Claude
  - Anthropic
  - Agent
  - Skills
  - MCP
  - Prompt
---
# 对比Skill, mcp等
## 一、核心结论

Claude 具备很强的通用能力，但在真实工作中，用户通常还需要它遵循特定的流程、使用特定的工具、参考特定的资料，并以符合组织要求的方式完成任务。

Anthropic 将这些能力拆分为几类不同的机制：

- **Prompt（提示词）**：告诉模型当前要完成什么任务。
- **Project（项目）**：为一组持续进行的工作提供共享上下文。
- **Skill（技能）**：把可重复使用的专业知识、流程和资源打包起来，并在需要时加载。
- **Subagent（子代理）**：为某个子任务启动一个拥有独立上下文的专门代理。
- **MCP（Model Context Protocol，模型上下文协议）**：以标准化方式把 Claude 连接到外部工具和数据。

这些机制解决的是不同层面的问题。它们不是相互替代的“同一种配置”，而是可以组合使用的不同构件。

## 二、先理解五个概念

### 1. Prompt：当前任务的指令

Prompt 是用户直接交给 Claude 的具体指令、问题或任务描述。

它适合表达：

- 这一次想完成什么；
- 输出需要采用什么格式；
- 当前任务有哪些限制；
- Claude 应该关注哪些信息。

Prompt 的作用范围通常是当前交互。它告诉 Claude **现在要做什么**，但不会自动把一套完整的专业流程沉淀为可复用能力。

### 2. Project：持续工作的共享上下文

Project 用于组织一组持续进行的相关工作。它可以让 Claude 在项目范围内参考共享的背景资料、项目说明和相关文件。

Project 适合回答：

- 这是哪一类长期工作；
- 项目的背景和边界是什么；
- 哪些资料属于这个项目；
- 在后续对话中应该持续保留哪些上下文。

Project 主要解决的是**上下文与工作范围**问题，不等于一套具体的操作流程。

### 3. Skill：可复用的专业工作方式

Skill 是一组可复用的指令、脚本和参考资料，用于让 Claude 在特定任务中采用专业、稳定、可重复的工作方式。

原文将 Skill 描述为一种“模块化能力”：它不是单纯的一段提示词，而是一个可以包含多个组成部分的目录或能力包。Skill 可以包含：

- 说明 Claude 应该如何工作的指令；
- 执行特定任务所需的脚本；
- 任务所需的参考资料；
- 用于特定输出格式或领域知识的资源。

Skill 的一个重要特点是**按需加载**。Claude 不必在每次对话开始时都把所有技能的全部内容放入上下文；当某项技能与当前任务相关时，Claude 可以加载它并应用其中的知识和流程。

因此，Skill 主要回答：

> **“完成这一类任务时，应该采用什么专业方法？”**

### 4. Subagent：独立处理子任务的代理

Subagent 是一个为特定子任务而配置的独立代理。它可以拥有自己的任务说明、工具和上下文，并在完成工作后把结果返回给主代理。

Subagent 适合处理：

- 可以明确拆分出来的子任务；
- 需要独立上下文的分析；
- 需要专门角色或专门工具的工作；
- 不希望主对话被大量中间细节占满的任务。

Subagent 主要回答：

> **“这项工作是否应该交给一个独立的专门代理来完成？”**

### 5. MCP：连接外部工具和数据的标准协议

MCP 是 **Model Context Protocol（模型上下文协议）** 的缩写。它是一种用于连接 Claude 与外部工具、服务和数据源的开放标准。

MCP 解决的是连接问题，例如：

- 让 Claude 访问外部数据库；
- 让 Claude 使用第三方服务；
- 让 Claude 读取或操作某个外部系统；
- 让工具提供结构化的资源、提示词或操作能力。

MCP 主要回答：

> **“Claude 如何获得外部世界中的数据和工具？”**

MCP 本身不是一种专业工作流程，也不是一个替代 Skill 的提示词容器。它提供的是标准化的连接方式。

## 三、Skill 到底是什么

### 3.1 Skill 的基本组成

一个 Skill 通常是一个目录，里面包含一个入口说明文件以及完成任务所需的其他资源。其内容可以包括：

| 组成部分 | 作用 |
| --- | --- |
| 指令 | 规定 Claude 在特定任务中应遵循的步骤、规则和判断标准 |
| 脚本 | 执行重复性、结构化或需要确定性处理的操作 |
| 参考资料 | 为 Claude 提供领域知识、规范、模板或背景信息 |
| 资源文件 | 支撑任务执行的其他文件，如示例、配置或模板 |

Skill 的价值不只在于“告诉 Claude 一句话怎么做”，而在于把完成一类任务所需的**知识、流程和工具**组织成一个可复用单元。

### 3.2 为什么需要 Skill

单次 Prompt 可以解决一次任务，但重复使用时通常会遇到几个问题：

1. 每次都需要重新描述同一套流程；
2. 不同人提供的指令不一致，输出质量容易波动；
3. 复杂任务需要同时提供大量背景资料；
4. 某些步骤适合用脚本执行，而不是依赖模型临场生成；
5. 专业规则、格式要求和质量标准难以长期维护。

Skill 可以把这些要求沉淀下来，使 Claude 在需要时采用一致的方法完成任务。

### 3.3 Skill 如何被使用

原文强调，Skill 是**渐进式披露（progressive disclosure）**的一种实践方式：

1. Claude 先判断当前任务是否与某个 Skill 相关；
2. 只有在相关时，才加载该 Skill 的说明和必要资源；
3. Claude 根据 Skill 中的流程和规则执行任务；
4. 如果 Skill 中包含脚本或其他工具，Claude 可以在适当的时候使用它们；
5. 任务完成后，Skill 不必永久占据每次对话的全部上下文。

这种方式的目标是：在不把所有知识一次性塞进上下文的前提下，让 Claude 在需要时获得更专业、更完整的能力。

## 四、五种机制的横向对比

### 4.1 总览表

| 机制 | 主要解决的问题 | 作用对象 | 是否强调复用 | 是否包含工具 / 文件 | 上下文特点 |
| --- | --- | --- | --- | --- | --- |
| Prompt | 当前要做什么 | 一次任务 | 低到中 | 通常不包含 | 依赖当前对话 |
| Project | 项目背景是什么 | 一组持续工作 | 中 | 可以包含资料 | 共享项目上下文 |
| Skill | 这类任务应该怎么做 | 一套专业流程 | 高 | 可以包含脚本和参考资料 | 按需加载 |
| Subagent | 哪个子任务由谁独立完成 | 一个专门子任务 | 中到高 | 可以配置专门工具 | 独立上下文 |
| MCP | 如何连接外部工具和数据 | 外部系统 | 高 | 核心就是工具 / 数据连接 | 提供外部上下文 |

### 4.2 按“任务生命周期”比较

| 工作阶段 | 更适合使用的机制 | 原因 |
| --- | --- | --- |
| 临时提出一个问题 | Prompt | 直接说明当前目标即可 |
| 为长期项目提供背景 | Project | 让相关对话共享项目资料和边界 |
| 固化一套重复流程 | Skill | 将流程、规则、脚本和参考资料封装起来 |
| 把复杂工作拆给专门角色 | Subagent | 用独立代理处理边界清晰的子任务 |
| 访问外部服务或数据 | MCP | 提供标准化的工具和数据连接 |

### 4.3 按“主要问题”比较

| 你遇到的问题 | 首选机制 | 典型问题表达 |
| --- | --- | --- |
| Claude 不知道我这次想要什么 | Prompt | “请把这段内容总结成三点。” |
| Claude 不知道项目背景 | Project | “这是一个持续推进的研究项目。” |
| Claude 每次做同类任务的方法不一致 | Skill | “每次都按这个研究、检查和输出流程完成。” |
| 一个任务太大，需要分工 | Subagent | “请让一个专门代理单独完成竞争对手分析。” |
| Claude 无法访问外部系统 | MCP | “请连接数据库、设计工具或业务系统。” |

## 五、容易混淆的概念：Skill、Prompt 与 Project

### 5.1 Skill 与 Prompt

二者都可以包含指令，但定位不同：

| 对比项 | Prompt | Skill |
| --- | --- | --- |
| 使用方式 | 用户在当前对话中直接提供 | 预先整理为可复用能力 |
| 适用范围 | 一次性或临时任务 | 一类反复出现的任务 |
| 内容规模 | 通常较短 | 可包含指令、脚本、参考资料和资源 |
| 触发方式 | 用户明确提出 | Claude 根据任务相关性按需加载 |
| 核心价值 | 表达当前意图 | 沉淀专业方法 |

可以把 Prompt 理解为“这一次的请求”，把 Skill 理解为“完成这类请求时应该遵循的工作方法”。

### 5.2 Skill 与 Project

Project 提供工作背景，Skill 提供完成任务的方法。

例如：

- Project 中可以保存一个公司的品牌资料、产品文档和市场背景；
- Skill 可以规定如何进行品牌审核、竞品研究或月度报告生成；
- Claude 在该 Project 中工作时，可以根据当前任务加载相关 Skill。

二者可以同时使用，也不应互相替代。

### 5.3 Skill 与 Subagent

Skill 是能力和工作方式；Subagent 是执行任务的独立代理。

一个 Subagent 可以使用一个或多个 Skill。例如：

- “研究代理”负责收集和分析竞争情报；
- “报告 Skill”规定最终报告的结构、引用格式和质量标准；
- “数据访问 MCP”提供外部数据源。

三者在系统中分别承担角色、方法和连接的职责。

## 六、一个完整的组合示例：竞争情报研究代理

原文通过一个竞争情报研究场景说明这些机制如何组合。

### 6.1 任务目标

假设团队希望 Claude 定期完成竞争情报研究，包括：

- 收集竞争对手的公开信息；
- 访问相关数据源；
- 分析变化和趋势；
- 按照团队要求生成报告；
- 让结果可以被后续工作继续使用。

### 6.2 各机制的分工

| 组件 | 在示例中的职责 |
| --- | --- |
| Prompt | 提出本次研究目标，例如研究某个竞争对手最近的产品变化 |
| Project | 提供公司背景、既有研究、产品资料和研究范围 |
| Skill | 规定研究步骤、判断标准、报告格式和引用要求 |
| Subagent | 独立执行竞争对手信息收集或分析工作 |
| MCP | 连接搜索、数据库、文档系统或其他外部数据源 |

### 6.3 组合后的工作过程

1. 用户通过 Prompt 提出本次研究任务；
2. Claude 从 Project 中获得项目背景和已有资料；
3. Claude 判断哪些 Skill 与任务相关，并按需加载；
4. Claude 通过 MCP 访问外部数据和工具；
5. Claude 将可独立拆分的工作交给 Subagent；
6. Subagent 返回研究结果；
7. 主代理按照 Skill 规定的方法整合、检查并输出报告；
8. 最终结果回到当前 Project，成为后续研究的参考资料。

这个例子说明：真正可用的 Agent 系统通常不是“只增加一个更长的 Prompt”，而是把**任务、背景、方法、分工和连接**分别组织起来。

## 七、如何判断应该使用哪一种机制

可以用下面的判断顺序：

1. **这是一次性的具体要求吗？**  
   使用 Prompt。
2. **这项工作是否属于一个持续的项目？**  
   使用 Project 保存共享背景。
3. **这类任务是否会反复出现，而且需要一致的方法？**  
   创建 Skill。
4. **这项工作能否拆成一个边界清晰的独立子任务？**  
   使用 Subagent。
5. **任务是否需要访问外部工具、服务或数据？**  
   使用 MCP。
6. **是否同时满足多个条件？**  
   组合使用，而不是强行选择单一机制。

## 八、重要理解：这些机制是互补关系

这五种机制可以看成不同层次的能力：

```text
Prompt   = 当前任务是什么
Project  = 工作背景是什么
Skill    = 这类任务应该怎么做
Subagent = 哪个子任务由独立代理完成
MCP      = 外部工具和数据如何接入
```

一个更完整的组合可以表示为：

```text
用户目标
  -> Prompt
  -> Project 提供背景
  -> Skill 提供方法
  -> Subagent 负责独立子任务
  -> MCP 提供外部工具和数据
  -> 主代理整合结果并完成输出
```

## 九、整理后的关键 takeaway

- **Prompt 是指令，不是完整能力。**
- **Project 是共享上下文，不是专业流程。**
- **Skill 是可复用的专业能力包，可以包含指令、脚本和参考资料。**
- **Skill 按需加载，有助于减少不必要的上下文占用。**
- **Subagent 适合独立处理边界清晰的子任务。**
- **MCP 是连接外部工具和数据的标准协议。**
- **这些机制可以组合使用，分别承担任务、背景、方法、分工和连接的职责。**
- **构建可靠的 Agent 工作流，关键不只是让模型“更聪明”，还要把工作方式组织得更清楚。**

来源：<https://claude.com/blog/skills-explained>

